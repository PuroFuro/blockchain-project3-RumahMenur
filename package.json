{
  "name": "voting-dapp",
  "version": "1.0.0",
  "description": "On-chain voting dApp (Solidity + Hardhat). Contract side.",
  "license": "MIT",
  "scripts": {
    "node": "hardhat node",
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "hardhat": "^2.28.6"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "type": "module"
}
